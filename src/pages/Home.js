import React, { useState } from 'react';
import Items from './Items';
import Header from './Header'
const Home = () => {

    return (
        <div>
            <Header></Header>
            <Items></Items>
        </div>
    );
};

export default Home;